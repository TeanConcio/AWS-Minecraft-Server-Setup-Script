sudo su - minecraft <command>
sudo -u minecraft bash -c 'command'
# Switch to created minecraft user

screen -ls
# List screens

screen -S/-r minecraft
# Create and switch to minecraft screen
# To detatch: Ctrl+A D

screen -S minecraft -X quit
# Quit screens

./<script>
# Run script

sudo nano /opt/minecraft/server/scripts/start_script.sh
# Modify file

sudo systemctl start/stop/status minecraft.service
# Start / Stop / Show Status of services

sudo systemctl enable/disable minecraft.service
# Enable/Disable service at startup

sudo systemctl list-units --type=service --state=running
# List all active services

journalctl -t minecraft-server
# View service logs

ps aux | grep java
# Check for java processes

sudo kill -9 <PID>
# Kill processes

top
# Check system CPU utilization

free -h
# Check system memory utilization

lsblk
df -h
# Check system disk utilization (Look for mount with EBS storage volume)

du -sh <Directory Path>
# Check disk utilization of directory

getent passwd | cut -d: -f1
# Get existing usernames

sudo setfacl -R -b /opt/minecraft
sudo chown -R :minecraft-group /opt/minecraft
sudo chmod -R 770 /opt/minecraft
sudo setfacl -R -m g:minecraft-group:rwx /opt/minecraft
sudo setfacl -R -m d:g:minecraft-group:rwx /opt/minecraft
# Give minecraft-group permissions